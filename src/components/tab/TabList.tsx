import {PropsWithChildren} from "react";

const TabList = ({
                     children
                 }: PropsWithChildren) => {
    return (
        <div>
            {children}
        </div>
    )
}

export default TabList;
